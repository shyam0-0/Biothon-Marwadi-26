package com.medfusion.ai.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * MedFusion palette — a calm, trustworthy medical-blue system.
 * Neutral surfaces, a single confident primary, and a small set of semantic
 * colors reused for urgency levels and confidence chips everywhere in the app.
 */

// Brand — medical blue
val Blue600 = Color(0xFF1E6FD9)
val Blue700 = Color(0xFF1857B0)
val Blue500 = Color(0xFF3B82F6)
val Blue100 = Color(0xFFDCE9FB)
val Blue50 = Color(0xFFEEF4FE)

// Accent — clinical teal
val Teal500 = Color(0xFF0EA5A5)
val Teal100 = Color(0xFFD1F1F1)

// Neutrals (light)
val Surface = Color(0xFFF6F9FE)
val SurfaceElevated = Color(0xFFFFFFFF)
val OnSurface = Color(0xFF0F1B2D)
val OnSurfaceVariant = Color(0xFF5A6B82)
val Outline = Color(0xFFD5DEEC)

// Neutrals (dark)
val SurfaceDark = Color(0xFF0B1220)
val SurfaceElevatedDark = Color(0xFF141E30)
val OnSurfaceDark = Color(0xFFE7EDF7)
val OnSurfaceVariantDark = Color(0xFF9BABC4)
val OutlineDark = Color(0xFF283549)

// Semantic — shared by urgency levels (Phase 2/7) and confidence (Phase 5)
val RiskGreen = Color(0xFF15A24A)
val RiskGreenContainer = Color(0xFFE1F6E9)
val RiskYellow = Color(0xFFD98A00)
val RiskYellowContainer = Color(0xFFFCF0D6)
val RiskRed = Color(0xFFD64545)
val RiskRedContainer = Color(0xFFFBE2E2)

// Dark-mode semantic containers (muted so they don't glare on dark surfaces)
val RiskGreenContainerDark = Color(0xFF12321F)
val RiskYellowContainerDark = Color(0xFF3A2E12)
val RiskRedContainerDark = Color(0xFF3A1C1C)

val ErrorRed = Color(0xFFD64545)
